/*

import React from 'react'

export function withMediaType(Component: any, mediaType: string) {
    return class extends React.Component {
      
      render() {
        return <Component type={mediaType} />
      }
    }
}





class Albums extends React.Component {
  render() {
    return (
      <>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
        <Text style={{ color: 'red' }}>{this.props.type}</Text>
      </>
    )
  }  
}

export const AlbumsScreen = withMediaType(Albums, 'albums')

class Videos extends React.Component {
  render() {
    <Text>{this.props.type}</Text>
  }
}

export const VideosScreen = withMediaType(Albums, 'videos')

*/
